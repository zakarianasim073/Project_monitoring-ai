# Palette's Journal

## 2026-05-15 - Initializing Palette Journal
**Learning:** Found critical syntax errors in `DocumentManager.tsx` where JSX was floating outside the component. Also identified missing accessibility features in `SmartUploadModal.tsx` (no close button, no ESC/backdrop dismissal).
**Action:** Will remediate these by moving JSX into scope and implementing the "Accessible Modal" pattern.
